# 如何确保团队成员数据库中的教师信息一致？

## 📌 问题背景

在协同开发中，每个开发者都有自己的本地数据库。如果：
- 开发者 A 在管理端添加了新教师
- 开发者 B 修改了某个教师的信息
- 开发者 C 删除了一些测试数据

**如何确保大家的数据库数据保持一致？**

## ✅ 解决方案

我为你的项目创建了一套完整的数据一致性保障系统！

### 🛠️ 核心工具

#### 1️⃣ `verify_teachers` - 数据验证工具
```bash
python manage.py verify_teachers
```

**功能：**
- 🔍 检查数据库与标准数据集（`teachers_data_final.json`）的一致性
- 📊 报告缺失的教师、额外的教师、内容差异
- 💡 提供具体的修复建议

**输出示例：**
```
📋 验证教师数据: teachers_data_final.json
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 数据统计:
   JSON 文件: 13 位教师
   数据库:   15 位教师

⚠️ 数据库中额外的教师 (2):
   - 张三
   - 李四

💡 建议操作:
   导出当前数据: python manage.py export_teachers
```

#### 2️⃣ `sync_teachers` - 数据同步工具
```bash
python manage.py sync_teachers --mode update --backup
```

**功能：**
- 🔄 将数据库同步到标准数据集
- 💾 自动备份当前数据
- 🎯 支持三种同步模式

**三种模式：**

| 模式 | 说明 | 使用场景 |
|------|------|----------|
| `update` | 更新现有+添加缺失 | ⭐ 日常同步（推荐） |
| `merge` | 保留额外的教师 | 有意添加了测试数据 |
| `reset` | 完全重置 | 数据严重混乱时 |

#### 3️⃣ `export_teachers` - 数据导出工具
```bash
python manage.py export_teachers --output my_data.json
```

**功能：**
- 📤 将当前数据库导出为 JSON
- 🔄 用于更新标准数据集
- 💼 用于分享测试数据

#### 4️⃣ `import_teachers` - 数据导入工具
```bash
python manage.py import_teachers --json-file custom_data.json
```

**功能：**
- 📥 从 JSON 文件导入教师数据
- 🔧 在 `setup_database.py` 中自动执行

## 🎯 实际使用流程

### 场景 1：每天开始开发前（2分钟）

```bash
# 1. 拉取最新代码
git pull

# 2. 更新数据库结构
cd backend
python manage.py migrate

# 3. 验证数据一致性 ⭐
python manage.py verify_teachers

# 4. 如果发现差异，同步数据
python manage.py sync_teachers --mode update --backup

# 5. 启动开发服务器
python manage.py runserver
```

### 场景 2：添加了新的教师数据，想分享给团队

```bash
# 开发者 A 在管理端添加了 3 位新教师

# 1. 导出当前数据
python manage.py export_teachers --output team_new_teachers.json

# 2. 更新标准数据集（如果团队同意）
python manage.py export_teachers \
  --output teachers_data_final.json \
  --overwrite

# 3. 提交到 Git
git add teachers_data_final.json
git commit -m "Add 3 new teachers: 张三, 李四, 王五"
git push

# 4. 通知团队成员
# "我添加了 3 位新教师，请大家 pull 后运行：
#  python manage.py sync_teachers --mode update"
```

### 场景 3：团队成员同步数据

```bash
# 开发者 B 收到通知后

# 1. 拉取最新代码
git pull

# 2. 检查差异
python manage.py verify_teachers

# 输出：
# ❌ 数据库中缺失的教师 (3):
#    - 张三
#    - 李四
#    - 王五

# 3. 同步数据
python manage.py sync_teachers --mode update --backup

# 4. 再次验证
python manage.py verify_teachers

# 输出：
# ✅ 完美！数据库与 JSON 文件完全一致
```

### 场景 4：新成员加入团队

```bash
# 1. 克隆项目
git clone <repository-url>
cd RateMyProf_OBU

# 2. 后端设置
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. 初始化数据库（会自动导入标准数据集）
python setup_database.py

# 4. 验证数据（应该显示完全一致）
python manage.py verify_teachers

# 输出应该是：
# ✅ 完美！数据库与 JSON 文件完全一致
```

## 📊 工作流程图

```
┌─────────────────────────────────────────────────────────────┐
│                     每日开发工作流                            │
└─────────────────────────────────────────────────────────────┘

1. git pull
        ↓
2. python manage.py migrate
        ↓
3. python manage.py verify_teachers
        ↓
   ┌────┴────┐
   │ 一致？   │
   └────┬────┘
        ↓
   ┌─────────┬─────────┐
   │   是    │   否     │
   ↓         ↓
   开始开发   执行同步
              ↓
              python manage.py sync_teachers --mode update
              ↓
              再次验证
              ↓
              开始开发


┌─────────────────────────────────────────────────────────────┐
│                   数据共享工作流                              │
└─────────────────────────────────────────────────────────────┘

开发者 A 修改数据
        ↓
python manage.py export_teachers --output teachers_data_final.json --overwrite
        ↓
git add + commit + push
        ↓
通知团队
        ↓
        ↓
开发者 B/C/D
        ↓
git pull
        ↓
python manage.py verify_teachers
        ↓
python manage.py sync_teachers --mode update
        ↓
✅ 所有人数据一致
```

## 🎓 核心原则

### 1. 数据库是单一数据源
- ✅ 应用运行时只读取数据库
- ✅ 管理端的修改直接写入数据库
- ✅ 这是正确的设计

### 2. JSON 文件是同步介质
- 📁 `teachers_data_final.json` = 团队约定的标准数据集
- 🔄 通过 Git 在团队间共享
- 🔍 通过 `verify_teachers` 检查一致性
- 🔄 通过 `sync_teachers` 同步差异

### 3. 经常验证，及时同步
```bash
# 建议加入每日习惯
git pull
python manage.py verify_teachers
# 一旦发现差异立即处理
```

### 4. 沟通协作
- 修改标准数据集前通知团队
- 在 commit message 中说明数据变更
- 定期团队同步会议确认数据状态

## 📋 检查清单

### 每天开发前
- [ ] `git pull` 拉取最新代码
- [ ] `python manage.py migrate` 更新数据库结构
- [ ] `python manage.py verify_teachers` 验证数据
- [ ] 如有差异，执行 `sync_teachers`

### 修改数据后
- [ ] 确认是临时测试数据还是团队共享数据
- [ ] 如需共享，使用 `export_teachers` 导出
- [ ] 更新 `teachers_data_final.json`
- [ ] 提交到 Git 并通知团队

### 提交代码前
- [ ] `python manage.py verify_teachers`
- [ ] 检查 git status，确认数据文件是否应该提交
- [ ] 如有数据变更，在 commit message 中说明

## 🚀 快速入门

### 5 秒检查数据
```bash
python manage.py verify_teachers
```

### 30 秒同步数据
```bash
python manage.py sync_teachers --mode update --backup
```

### 1 分钟完整流程
```bash
git pull
cd backend
python manage.py migrate
python manage.py verify_teachers
python manage.py sync_teachers --mode update --backup
python manage.py runserver
```

## 📚 更多资源

| 文档 | 内容 | 何时查看 |
|------|------|---------|
| [QUICK_REFERENCE.md](QUICK_REFERENCE.md) | 常用命令速查表 | ⭐ 每天使用 |
| [TEAM_WORKFLOW.md](TEAM_WORKFLOW.md) | 完整团队协作流程 | 遇到复杂情况时 |
| [DATA_MANAGEMENT_GUIDE.md](DATA_MANAGEMENT_GUIDE.md) | 数据管理深入指南 | 理解原理时 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 系统架构说明 | 了解设计决策时 |
| [README.md](README.md) | 项目总体说明 | 项目概览 |

## ✨ 总结

通过这套工具，你可以：

1. **轻松检测数据不一致** - `verify_teachers` 一键检查
2. **快速同步团队数据** - `sync_teachers` 自动同步
3. **安全备份数据** - 所有操作都支持 `--backup`
4. **灵活导入导出** - 支持自定义数据集

**最佳实践：**
```bash
# 把这个加入你的每日习惯
git pull && cd backend && python manage.py verify_teachers
```

**记住：**
> 数据库是真相，JSON 是快照
> 经常验证，及时同步
> 沟通协作，避免冲突

🎉 现在你的团队可以放心协同开发了！

